# 解題思路

### 目的：
移除陣列 nums 中所有等於 val 的元素，並返回新的長度 k，使得 nums 的前 k 個元素都不等於 val。後面的元素則無關緊要。
### 方法：
- 使用雙指針技術，i 用來遍歷陣列，k 用來標記不等於 val 的元素應放置的位置。
- 當遍歷到一個不等於 val 的元素時，將其移動到 k 的位置，並將 k 進行遞增。
### Language：
Java
### Runtime：
0 ms
### Memory： 
41.52 MB
